# Guardian Reports - Ignore temporary/test reports

# Test reports
*_test_*.json
*_test.json

# Temporary consolidated reports (keep latest only)
consolidated_report_*.json

# Keep these core reports (tracked in git)
!prod_report.json
!docs_report.json
!integrity_report.json
!unified_report.json
!global_report.json
!codex_summary.md

# AI cost audits (keep last 2 months)
# ai_model_cost_audit_*.json are OK to track

# Archive folder is tracked
!archive/
!archive/**
